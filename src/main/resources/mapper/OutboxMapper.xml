<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minipay.infra.outbox.OutboxMapper">

    <!-- 批量插入本地消息：MyBatis foreach -->
    <insert id="insertBatch">
        INSERT INTO t_message_outbox
        (id, event_id, trace_id, service, event_type, payload, status, retry_count, next_retry_time,
         version, created_at, updated_at)
        VALUES
        <foreach collection="items" item="it" separator=",">
            (#{it.id}, #{it.eventId}, #{it.traceId}, #{it.service}, #{it.eventType}, #{it.payload}, #{it.status},
             #{it.retryCount}, #{it.nextRetryTime}, #{it.version}, #{it.createdAt}, #{it.updatedAt})
        </foreach>
    </insert>

    <!-- 批量标记已投递 -->
    <update id="markSent">
        UPDATE t_message_outbox
        SET status = 'SENT', updated_at = #{updatedAt}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!-- 投递失败：更新重试信息 -->
    <update id="updateRetry">
        UPDATE t_message_outbox
        SET retry_count = #{retryCount}, status = #{status},
            next_retry_time = #{nextRetryTime}, updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

</mapper>
