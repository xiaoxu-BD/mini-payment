<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minipay.recon.mapper.ReconTaskMapper">

    <!-- 对账结束：落最终结果 -->
    <update id="finishTask">
        UPDATE t_recon_task
        SET bill_file = #{billFile}, total_count = #{totalCount}, matched_count = #{matchedCount},
            diff_count = #{diffCount}, status = #{status}, finished_at = #{finishedAt},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

</mapper>
